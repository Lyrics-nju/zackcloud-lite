import type { StatusCounts, UserRecord } from "./types";

export const PORTAL_CSS = `
:root{color-scheme:light;--ink:#172033;--muted:#657188;--line:#dfe5ee;--panel:#fff;--soft:#f4f7fb;--brand:#1266d4;--brand2:#0b4da7;--ok:#087a55;--warn:#a45b00;--bad:#bd2c2c;--shadow:0 18px 50px rgba(24,45,78,.1)}
*{box-sizing:border-box}body{margin:0;background:#f7f9fc;color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI","Microsoft YaHei",sans-serif;line-height:1.6}a{color:var(--brand);text-decoration:none}button,input{font:inherit}.shell{width:min(1120px,100%);margin:auto;padding:0 20px}.nav{height:68px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);background:#ffffffed;backdrop-filter:blur(12px);position:sticky;top:0;z-index:10}.brand{display:flex;align-items:center;gap:11px;font-weight:800;color:var(--ink);letter-spacing:-.02em}.logo{display:grid;place-items:center;width:34px;height:34px;border-radius:11px;background:var(--brand);color:#fff;font-weight:900}.navlinks{display:flex;align-items:center;gap:14px}.hero{display:grid;grid-template-columns:1.2fr .8fr;align-items:center;gap:56px;min-height:calc(100vh - 68px);padding:70px 0}.eyebrow{color:var(--brand);font-weight:750}.hero h1{font-size:clamp(42px,7vw,74px);line-height:1.04;letter-spacing:-.055em;margin:12px 0 22px}.lead{font-size:18px;color:var(--muted);max-width:640px}.actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:30px}.button{display:inline-flex;align-items:center;justify-content:center;min-height:44px;padding:0 18px;border-radius:11px;border:1px solid var(--line);background:#fff;color:var(--ink);font-weight:700;cursor:pointer}.button.primary{background:var(--brand);border-color:var(--brand);color:#fff}.button.danger{border-color:#f0c7c7;color:var(--bad)}.button:disabled{opacity:.55;cursor:not-allowed}.hero-card,.card{background:var(--panel);border:1px solid var(--line);border-radius:20px;box-shadow:var(--shadow)}.hero-card{padding:28px}.steps{display:grid;gap:18px}.step{display:grid;grid-template-columns:34px 1fr;gap:12px}.step b{display:grid;place-items:center;width:30px;height:30px;border-radius:50%;background:#e8f1ff;color:var(--brand)}.page{padding:44px 0 80px}.page-head{display:flex;align-items:flex-end;justify-content:space-between;gap:20px;margin-bottom:26px}.page h1{font-size:32px;letter-spacing:-.035em;margin:0}.subtle{color:var(--muted)}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.card{padding:24px}.card h2{font-size:18px;margin:0 0 18px}.form{display:grid;gap:15px}.field{display:grid;gap:7px}.field label{font-weight:700;font-size:14px}.field input{width:100%;height:46px;border:1px solid #cdd6e4;border-radius:10px;padding:0 13px;background:#fff}.field input:focus{outline:3px solid #cfe3ff;border-color:var(--brand)}.auth-wrap{width:min(480px,100%);margin:64px auto}.message{padding:13px 15px;border-radius:10px;background:#eef4fd;color:#25466f;margin-bottom:18px}.message.error{background:#fff0f0;color:#8e2525}.status{display:inline-flex;align-items:center;border-radius:999px;padding:5px 10px;font-size:12px;font-weight:800;background:#e9eef5}.status.APPROVED{background:#def7ed;color:var(--ok)}.status.PENDING{background:#fff1d8;color:var(--warn)}.status.DISABLED,.status.REJECTED{background:#ffe4e4;color:var(--bad)}.masked{padding:14px;border:1px dashed #bcc8d8;border-radius:10px;background:var(--soft);font-family:ui-monospace,SFMono-Regular,Consolas,monospace;overflow-wrap:anywhere}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-bottom:20px}.stat{background:#fff;border:1px solid var(--line);border-radius:15px;padding:18px}.stat strong{display:block;font-size:28px}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:14px}table{width:100%;border-collapse:collapse;background:#fff}th,td{text-align:left;padding:13px;border-bottom:1px solid var(--line);white-space:nowrap}th{font-size:12px;color:var(--muted);text-transform:uppercase}.row-actions{display:flex;gap:7px}.row-actions .button{min-height:34px;padding:0 10px;font-size:12px}.empty{padding:36px;text-align:center;color:var(--muted)}.guide{padding-left:20px;color:var(--muted)}.toast{position:fixed;right:20px;bottom:20px;padding:13px 16px;background:#172033;color:#fff;border-radius:11px;box-shadow:var(--shadow);opacity:0;transform:translateY(8px);pointer-events:none;transition:.2s}.toast.show{opacity:1;transform:none}dialog{border:0;border-radius:16px;padding:0;box-shadow:0 30px 90px #0004;width:min(420px,calc(100% - 32px))}dialog::backdrop{background:#14213d66}.dialog-body{padding:24px}.dialog-actions{display:flex;justify-content:flex-end;gap:10px;margin-top:22px}
.nav-inner{display:flex;align-items:center;justify-content:space-between}.section-gap{margin-top:18px}.row-actions form{display:flex;gap:6px}.row-actions input{width:150px;border:1px solid var(--line);border-radius:8px;padding:0 8px}
@media(max-width:760px){.hero{grid-template-columns:1fr;padding:44px 0}.hero-card{display:none}.grid{grid-template-columns:1fr}.stats{grid-template-columns:repeat(2,1fr)}.navlinks .hide-mobile{display:none}.page-head{align-items:flex-start;flex-direction:column}.row-actions{flex-wrap:wrap}}
`;

export const PORTAL_JS = `
const toast=document.querySelector('[data-toast]');function notify(message){if(!toast)return;toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2400)}
document.querySelectorAll('[data-subscription]').forEach(card=>{const value=card.querySelector('[data-value]');const copy=card.querySelector('[data-copy]');const reveal=card.querySelector('[data-reveal]');let real=null;async function load(){if(real)return real;copy.disabled=true;copy.textContent='正在获取…';try{const response=await fetch('/api/subscription',{headers:{accept:'application/json'}});if(!response.ok)throw new Error();real=(await response.json()).url;return real}finally{copy.disabled=false;copy.textContent='复制订阅地址'}}reveal?.addEventListener('click',async()=>{try{const url=await load();const hidden=value.dataset.hidden==='true';value.textContent=hidden?url:'https://sub.zackcloud.site/sub/••••••••';value.dataset.hidden=hidden?'false':'true';reveal.textContent=hidden?'隐藏':'显示'}catch{notify('暂时无法获取订阅地址')}});copy?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(await load());notify('订阅地址已复制')}catch{notify('复制失败，请稍后重试')}})});
const dialog=document.querySelector('[data-confirm-dialog]');let pending=null;document.querySelectorAll('form[data-confirm]').forEach(form=>form.addEventListener('submit',event=>{if(form.dataset.confirmed==='true')return;event.preventDefault();pending=form;dialog?.showModal()}));dialog?.querySelector('[data-cancel]')?.addEventListener('click',()=>{pending=null;dialog.close()});dialog?.querySelector('[data-confirm]')?.addEventListener('click',()=>{if(!pending)return;pending.dataset.confirmed='true';pending.submit()});
`;

export function escapeHtml(value: unknown): string {
  return String(value ?? "").replace(/[&<>"']/g, (character) => ({
    "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;",
  })[character] ?? character);
}

function nav(auth: { label: string; admin?: boolean } | undefined, csrf: string): string {
  return `<header class="nav"><div class="shell nav-inner"><a class="brand" href="/"><span class="logo">Z</span><span>ZackCloud · 扎克云</span></a><nav class="navlinks">${auth ? `<span class="subtle hide-mobile">${escapeHtml(auth.label)}</span><form method="post" action="${auth.admin ? "/admin/logout" : "/logout"}"><input type="hidden" name="csrf" value="${escapeHtml(csrf)}"><button class="button" data-logout>退出</button></form>` : `<a href="/login">登录</a><a class="button primary" href="/register">申请注册</a>`}</nav></div></header>`;
}

export function layout(title: string, body: string, options: { auth?: { label: string; admin?: boolean }; csrf?: string; turnstile?: boolean } = {}): string {
  const csrf = escapeHtml(options.csrf ?? "");
  const turnstile = options.turnstile
    ? `<script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>` : "";
  return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(title)} · ZackCloud</title><link rel="stylesheet" href="/assets/portal.css">${turnstile}</head><body>${nav(options.auth, csrf)}${body}<div class="toast" data-toast role="status"></div><dialog data-confirm-dialog><div class="dialog-body"><h2>确认执行此操作？</h2><p class="subtle">此操作会立即影响账号或订阅凭据。</p><div class="dialog-actions"><button class="button" data-cancel>取消</button><button class="button danger" data-confirm>确认</button></div></div></dialog><script src="/assets/portal.js" defer></script></body></html>`;
}

export function homePage(): string {
  return layout("私人订阅服务", `<main class="shell hero"><section><div class="eyebrow">PRIVATE FRIEND SERVICE</div><h1>简单、安全地使用你的专属订阅。</h1><p class="lead">ZackCloud 为受邀朋友整理并分发 Mihomo / Clash 订阅。代理连接仍由你的客户端直接建立，Portal 不承载代理流量。</p><div class="actions"><a class="button primary" href="/register">申请加入</a><a class="button" href="/login">已有账号，登录</a></div></section><aside class="hero-card"><h2>开通流程</h2><div class="steps"><div class="step"><b>1</b><div><strong>提交申请</strong><div class="subtle">创建账号并等待审核</div></div></div><div class="step"><b>2</b><div><strong>管理员确认</strong><div class="subtle">审核通过后生成专属凭据</div></div></div><div class="step"><b>3</b><div><strong>复制订阅</strong><div class="subtle">导入 Mihomo-compatible 客户端</div></div></div></div></aside></main>`);
}

function authPage(kind: "register" | "login" | "admin", csrf: string, message?: string, invite = false, siteKey?: string): string {
  const register = kind === "register";
  const admin = kind === "admin";
  const action = admin ? "/admin/login" : register ? "/register" : "/login";
  const fields = register ? `<div class="field"><label>显示名称</label><input name="displayName" required maxlength="50" autocomplete="name"></div>` : "";
  const confirm = register ? `<div class="field"><label>确认密码</label><input type="password" name="passwordConfirm" required maxlength="128" autocomplete="new-password"></div>` : "";
  const invitation = invite ? `<div class="field"><label>邀请码</label><input name="inviteCode" required autocomplete="off"></div>` : "";
  const turnstile = siteKey ? `<div class="cf-turnstile" data-sitekey="${escapeHtml(siteKey)}"></div>` : "";
  return layout(admin ? "管理员登录" : register ? "申请注册" : "登录", `<main class="shell page"><section class="auth-wrap card"><h1>${admin ? "管理员登录" : register ? "申请加入 ZackCloud" : "欢迎回来"}</h1><p class="subtle">${register ? "提交后需要管理员审核，不会自动开通。" : "使用你的 ZackCloud 账号继续。"}</p>${message ? `<div class="message ${message.startsWith("错误") ? "error" : ""}">${escapeHtml(message)}</div>` : ""}<form class="form" method="post" action="${action}"><input type="hidden" name="csrf" value="${escapeHtml(csrf)}"><div class="field"><label>用户名</label><input name="username" required minlength="3" maxlength="32" autocomplete="username"></div>${fields}<div class="field"><label>密码</label><input type="password" name="password" required minlength="12" maxlength="128" autocomplete="${register ? "new-password" : "current-password"}"></div>${confirm}${invitation}${turnstile}<button class="button primary" type="submit">${register ? "提交申请" : "登录"}</button></form>${!admin ? `<p class="subtle">${register ? "已有账号？" : "还没有账号？"} <a href="${register ? "/login" : "/register"}">${register ? "去登录" : "申请注册"}</a></p>` : ""}</section></main>`, { csrf, turnstile: Boolean(siteKey) });
}

export const registerPage = (csrf: string, message?: string, invite = false, siteKey?: string) => authPage("register", csrf, message, invite, siteKey);
export const loginPage = (csrf: string, message?: string) => authPage("login", csrf, message);
export const adminLoginPage = (csrf: string, message?: string) => authPage("admin", csrf, message);

export function userDashboard(user: UserRecord, csrf: string): string {
  const statusText = { PENDING: "账号正在等待审核", APPROVED: "服务状态：已开通", REJECTED: "申请未通过", DISABLED: "账号当前已停用" }[user.status];
  const approved = user.status === "APPROVED" && (!user.expiresAt || Date.parse(user.expiresAt) > Date.now());
  const subscription = approved ? `<section class="card" data-subscription><h2>专属订阅</h2><div class="masked" data-value data-hidden="true">https://sub.zackcloud.site/sub/••••••••</div><div class="actions"><button class="button" data-reveal>显示</button><button class="button primary" data-copy>复制订阅地址</button></div></section>` : `<section class="card"><h2>订阅服务</h2><div class="empty">账号开通后，订阅入口会显示在这里。</div></section>`;
  return layout("用户中心", `<main class="shell page"><div class="page-head"><div><div class="eyebrow">USER DASHBOARD</div><h1>你好，${escapeHtml(user.displayName)}</h1></div></div><div class="grid"><section class="card"><h2>账号状态</h2><span class="status ${user.status}">${escapeHtml(statusText)}</span><p class="subtle">到期时间：${user.expiresAt ? escapeHtml(user.expiresAt) : "永久"}</p></section>${subscription}<section class="card"><h2>导入说明</h2><ol class="guide"><li>复制订阅地址</li><li>打开 Clash Verge Rev、Mihomo Party 或其他 Mihomo-compatible 客户端</li><li>添加订阅并粘贴地址</li><li>更新订阅后选择节点</li></ol></section><section class="card"><h2>隐私说明</h2><p class="subtle">Portal 不显示或推测流量、余额、延迟和在线状态，也不承载实际代理流量。</p></section></div></main>`, { auth: { label: user.username }, csrf });
}

function actionForm(user: UserRecord, action: string, csrf: string, label: string, destructive = false): string {
  return `<form method="post" action="/admin/users/${encodeURIComponent(user.id)}/${action}" ${destructive ? "data-confirm" : ""}><input type="hidden" name="csrf" value="${escapeHtml(csrf)}">${action === "delete" ? '<input type="hidden" name="confirm" value="yes">' : ""}${action === "expire" ? '<input name="expiresAt" aria-label="到期时间" placeholder="never 或 ISO 时间" required>' : ""}<button class="button ${destructive ? "danger" : ""}">${escapeHtml(label)}</button></form>`;
}

export function adminDashboard(users: UserRecord[], counts: StatusCounts, csrf: string, admin: string): string {
  const pending = users.filter((user) => user.status === "PENDING");
  const rows = (items: UserRecord[], pendingOnly = false) => items.length ? items.map((user) => `<tr><td>${escapeHtml(user.username)}</td><td>${escapeHtml(user.displayName)}</td><td><span class="status ${user.status}">${user.status}</span></td><td>${escapeHtml(user.expiresAt ?? "永久")}</td><td>${escapeHtml(user.createdAt)}</td><td><div class="row-actions">${pendingOnly ? `${actionForm(user, "approve", csrf, "通过")}${actionForm(user, "reject", csrf, "拒绝", true)}` : `${user.status === "DISABLED" ? actionForm(user, "enable", csrf, "恢复") : actionForm(user, "disable", csrf, "停用", true)}${actionForm(user, "expire", csrf, "更新到期")}${actionForm(user, "rotate", csrf, "重置订阅", true)}${actionForm(user, "delete", csrf, "删除", true)}`}</div></td></tr>`).join("") : `<tr><td colspan="6"><div class="empty">暂无记录</div></td></tr>`;
  return layout("管理后台", `<main class="shell page"><div class="page-head"><div><div class="eyebrow">ADMIN CONSOLE</div><h1>用户与申请</h1></div></div><section class="stats"><div class="stat"><span class="subtle">待审核</span><strong>${counts.pending}</strong></div><div class="stat"><span class="subtle">已开通</span><strong>${counts.approved}</strong></div><div class="stat"><span class="subtle">已停用</span><strong>${counts.disabled}</strong></div><div class="stat"><span class="subtle">总用户</span><strong>${counts.total}</strong></div></section><section class="card"><h2>待审核申请</h2><div class="table-wrap"><table><thead><tr><th>用户名</th><th>显示名称</th><th>状态</th><th>到期</th><th>申请时间</th><th>操作</th></tr></thead><tbody>${rows(pending, true)}</tbody></table></div></section><section class="card section-gap"><h2>全部用户</h2><div class="table-wrap"><table><thead><tr><th>用户名</th><th>显示名称</th><th>状态</th><th>到期</th><th>创建时间</th><th>操作</th></tr></thead><tbody>${rows(users)}</tbody></table></div></section></main>`, { auth: { label: admin, admin: true }, csrf });
}
